import styled from "styled-components";

export default styled.nav`
  width: fit-content;
  margin: 0 auto;

  & ul {
    list-style: none;
  }

  & a {
    text-decoration: none;
    font-size: 1.5rem;
    text-align: center;
    color: inherit;
  }
`;
